# Use a base image that includes a compatible JDK and Gradle for development.
FROM gradle:8.5-jdk17-alpine

# Set the working directory inside the container.
WORKDIR /app

# The default command to run when the container starts.
# We use "./gradlew bootRun" which starts the application in-place.
# This works with the volume mount in docker-compose.yml and enables
# Spring Boot DevTools for automatic hot-reloading when you change a file.
CMD ["./gradlew", "bootRun"]

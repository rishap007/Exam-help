# Use the official Gradle image with JDK 17
FROM gradle:8.5-jdk17-alpine

# Set the working directory inside the container
WORKDIR /app

# Copy only the Gradle wrapper files.
# The source code will be mounted as a volume from the host.
COPY build.gradle settings.gradle gradlew* ./
COPY gradle gradle

# Set the default command to run the application with hot-reloading.
# Gradle will download dependencies on the first run.
CMD ["./gradlew", "bootRun"]

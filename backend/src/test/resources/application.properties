# This tells your tests how to connect to the services running in Docker Compose

# Database Configuration
spring.datasource.url=jdbc:postgresql://localhost:5432/eduplatform_dev
spring.datasource.username=eduplatform_user
spring.datasource.password=dev_pg_pass_123

# Redis Configuration
spring.redis.host=localhost
spring.redis.port=6379

# Ensure Flyway runs migrations for the test database
spring.flyway.enabled=true

# This can help avoid some startup issues during tests
spring.jpa.properties.jakarta.persistence.validation.mode=none